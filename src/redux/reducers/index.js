const movesState = {
  main: {
    liked: false,
  },
};

const LikedMovesReducer = (state = movesState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default LikedMovesReducer;
